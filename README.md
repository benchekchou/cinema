# 🎬 Cinema Management System

This project is a **Cinema Management Application** developed with **Spring Boot** and **Spring Data JPA**. It allows managing cities, cinemas, halls, seats, showtimes, movies, categories, screenings, and tickets.

## 🚀 Key Features

- Automatically generates a cinema database with cities, cinemas, halls, seats, sessions, categories, movies, screenings, and tickets.
- REST API for accessing and manipulating core entities.
- Uses Spring Data JPA for data persistence.
- Embedded **H2** database for easy testing and initialization.

## 🛠️ Main Dependencies

- Java 17  
- Spring Boot (Web, Data JPA, Data REST)  
- H2 Database  
- Lombok  

## ⚙️ Installation & Running the App

1. **Clone the repository:**
   ```bash
   git clone https://github.com/benchekchou/cinema.git
